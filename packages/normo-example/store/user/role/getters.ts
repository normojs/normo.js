// export default {
//   getUserRoleInfo: (state) => {
//     return `${state.path}-user  8 role getters-${Math.random()}`
//   }
// }

export const getUserRoleInfo = (state: any) => {
  // const reust = state.path + '-user 3 role getters-' + Math.random() + '--2'
  return '0252445'
}
